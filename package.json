{
  "name": "health-watchers-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "turbo": "latest"
  },
  "scripts": {
    "prepare": "husky"
  },
  "dependencies": {
    "jest": "30.3.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["npx eslint --max-warnings 0", "npx prettier --write"],
    "*.{js,json,md,yml,yaml}": ["npx prettier --write"]
  }
}
